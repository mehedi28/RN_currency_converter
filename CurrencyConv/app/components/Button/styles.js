import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
    container:{
        alignItems: 'center',
        marginTop: 20
    },
    wrapper:{
        flexDirection: 'row',
        alignItems: 'center'
    },
    icon:{
        width: 19,
        height: 11
    }
})

export default styles
